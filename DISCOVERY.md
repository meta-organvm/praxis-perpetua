# Discovery: organvm/praxis-perpetua

**Date:** 2026-06-22 · **Verdict:** VALUE FOUND — promote to ranked tier (not archival)

## Value Thesis

`praxis-perpetua` (the Studium Generale ORGANVM) is the estate's **methodology and
certification authority**, and its highest latent value is a packaged, externally
publishable *operating system for agent-run institutions*: 74 governing standards plus
121 active SOPs distilled from real multi-agent sessions, a living `lessons/derived-principles`
corpus, and — uniquely — the **IRA multi-model evaluative-consensus protocol** (an 8-stage
defense pipeline with ICC thresholds: Paper > 0.61, Thesis > 0.70, Dissertation > 0.75 + Provost).
This is simultaneously (1) a **reusable internal capability** the rest of the estate already
depends on — any organ commissions, evaluates, and certifies knowledge work through it, with
549 cross-organ links and `consumes ← all-organs: research-commissions` already wired — and
(2) an **IP / publication revenue path**: the same machinery produces defended dissertations
(SGO-2026-D-001/002), arXiv-formatted preprints, and the Object Lessons publication, all flowing
out through ORGAN-V (Logos) and ORGAN-VII (Kerygma). The differentiated, hard-to-replicate asset
is not any single document but the *certification mechanism itself* — multi-model peer review as a
callable quality gate for AI-generated work — wrapped in a battle-tested corpus that documents how
to run an autonomous research-and-production institution. This is not archival; it is GRADUATED,
live, and load-bearing for the whole estate. Its value has simply never been *named and ranked*.

## Why not archival

- **Actively consumed:** `seed.yaml` declares `consumes ← all-organs: research-commissions`;
  CLAUDE.md reports 549 cross-organ links. Other organs route work *through* this repo.
- **Live capability, not a museum:** the IRA defense protocol, faculty registry, and senate
  config are operational governance, shared with `auto-revision-epistemic-engine`.
- **Produces outward IP:** publications → Logos, research-distribution → Kerygma,
  arXiv preprints → external, portfolio-evidence → application-pipeline.

## Highest latent value (ranked)

1. **IRA certification protocol as a reusable service** — multi-model evaluative consensus
   (ICC-gated) usable by any repo to certify a knowledge artifact. The estate's "immune system."
2. **SGO Method corpus** — 74 standards + 121 SOPs as a distributable handbook / IP for running
   agent institutions (publishable through the existing Logos/arXiv channel).
3. **prompt-corpus** — extracted, sequenced prompt/seed dataset; reusable training/eval material.

## Single best concrete first task

**Distill the IRA defense protocol + the canonical standards into one citable "SGO Method"
white paper, defended through the SGO's own pipeline and shipped to the Logos/arXiv channel.**
This converts scattered governance YAMLs and 74 markdown SOPs into a single distributable,
defensible asset — the artifact a new organ (or an external reader) actually consumes — and
dogfoods the certification machinery that is the repo's core value. It is concrete, builds on
existing infrastructure (`governance/defense-protocol.yaml`, `strategy/defense-rubrics/`,
`publications/arxiv/`), and produces an externally legible deliverable.
